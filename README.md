# MCP Audit

Real-time MCP token tracking for Claude Code, Codex CLI, and Gemini CLI.

**This repository is auto-populated from releases. See [PyPI](https://pypi.org/project/mcp-audit/) for installation.**

```bash
pip install mcp-audit
```
